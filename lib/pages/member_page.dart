import 'package:flutter/material.dart';

class MmemberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('会员中心'),
      ),
    );
  }
}
